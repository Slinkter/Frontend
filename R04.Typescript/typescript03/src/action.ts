export function sayHello(name: string): void {
    console.log(` heloe ${name}`);
}

export let person = "susan";

type Student = {
    name: string;
    age: number;
};

const newStuden: Student = {
    name: "peter",
    age: 1,
};

export default newStuden;

import React from "react";

/**
 * Renders an error message to the user when something goes wrong with TODO operations.
 * This component provides feedback that an unexpected issue has occurred.
 *
 * @returns {React.ReactElement} The error message UI.
 */
function TodosError() {
    return <p>Desespérate, hubo un error...</p>;
}

export { TodosError };

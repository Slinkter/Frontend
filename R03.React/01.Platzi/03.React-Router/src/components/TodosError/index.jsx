import React from "react";

/**
 * Componente visual de error.
 * Se muestra cuando ocurre un fallo crítico al cargar los datos.
 * 
 * @returns {JSX.Element} Mensaje de error para el usuario.
 */
function TodosError() {
    return <p>Desespérate, hubo un error...</p>;
}

export { TodosError };

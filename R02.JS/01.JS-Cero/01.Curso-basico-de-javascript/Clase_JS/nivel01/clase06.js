const sumar = (num1, num2) => {
  document.write(
    `<br>  num1 : ${num1} y num2 : ${num2}   =  ` + " " + (num1 + num2) + "<br>"
  );
};

sumar(1, 2);
sumar(2, 2);
sumar(3, 2);
sumar(4, 2);
sumar(5, 2);
sumar(6, 2);

import React from "react";

const InputBox = ({
  label,
  amout,
  onAmountChange,
  onCurrencyChange,
  currencyOptions = [],
  selectedCurrency = "usd",
  amountDisabled = false,
  currentcyDisabled = false,
  className = "",
}) => {
  return <div>InputBox</div>;
};

export default InputBox;
